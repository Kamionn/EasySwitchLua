# Build artifacts — regenerated by `lua build.lua` and attached to GitHub
# releases by the release.yml workflow. No reason to track them in source.
dist/

# Bench results (the bench scripts themselves stay tracked).
test_bench/results*.md
test_bench/*.md.stats.lua

# Lua compiled bytecode.
*.luac

# Editors / OS noise.
.vscode/
.idea/
.DS_Store
Thumbs.db
